{
  "name": "promises-in-batches",
  "version": "1.0.0",
  "description": "Execute promises sequentially in batches",
  "main": "./lib/index.js",
  "types": "./lib/index.d.ts",
  "author": "morishin",
  "homepage": "https://github.com/morishin/promises-in-batches",
  "repository": {
    "type": "git",
    "url": "git@github.com:morishin/promises-in-batches.git"
  },
  "license": "MIT",
  "private": false,
  "devDependencies": {
    "@types/jest": "^24.0.0",
    "jest": "^23.6.0",
    "prettier": "^1.16.4",
    "ts-jest": "^23.10.5",
    "typescript": "^3.3.3"
  },
  "scripts": {
    "prettier:check": "prettier --list-different '**/*.ts' '**/*.tsx'",
    "prettier:format": "prettier --write '**/*.ts' '**/*.tsx'",
    "test": "jest",
    "typecheck": "tsc --noEmit",
    "typecheck:watch": "tsc --noEmit --watch",
    "prepublish": "tsc"
  }
}
